export type Employee = {
  nama: string;
  divisi: string;
  jabatan: string;
  email: string;
};

export type LeaveTypes = {
  id: string;
  nama: string;
  divisi: string;
  tgl_pengajuan: string;
  tgl_cuti: string;
  alasan: string;
  status: string;
};
